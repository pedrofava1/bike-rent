export class Bike {
    constructor(
        name: string,
        type: string,
        bodySize: number,
        maxLoad: number,
        rate: number,
        description: string,
        ratings: number,
        imageUrls: string[],
        id?: string
    ) {}
}